# Streamlit demo

Run from the project root:

```bash
streamlit run deployment/app.py
```

The app estimates probability of default and displays a demonstration risk band. It is **not** intended for real lending decisions.
